# Hugging Face Spaces 根目录 requirements.txt
# HF Spaces 会自动安装此文件中的依赖
# 内容与 backend/requirements.txt 保持一致，并额外添加 aiofiles（静态文件服务）

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
edge-tts>=6.1.9
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0
pydantic>=2.0.0
aiofiles>=23.0.0
httpx>=0.27.0
